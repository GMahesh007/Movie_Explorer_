# 🏗️ OMDB Movie Explorer - Architecture Diagram

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│                      (http://localhost:3000)                     │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTP Requests
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      REACT FRONTEND (Vite)                       │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │    Pages     │  │  Components  │  │    Hooks     │          │
│  ├──────────────┤  ├──────────────┤  ├──────────────┤          │
│  │ - Home       │  │ - Header     │  │ - Favorites  │          │
│  │ - Detail     │  │ - SearchBar  │  └──────────────┘          │
│  │ - Favorites  │  │ - MovieCard  │                             │
│  └──────────────┘  │ - MovieGrid  │  ┌──────────────┐          │
│                    │ - Pagination │  │   Services   │          │
│                    └──────────────┘  ├──────────────┤          │
│                                      │ - API Client │          │
│                                      │ - Axios      │          │
│                                      └──────────────┘          │
│                                                                   │
│  Storage: localStorage (Favorites)                               │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ API Calls
                             │ /api/movies/*
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   EXPRESS.JS BACKEND (Node.js)                   │
│                      (http://localhost:5000)                     │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                     Middleware Stack                      │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ 1. Helmet (Security Headers)                             │   │
│  │ 2. CORS (Cross-Origin)                                   │   │
│  │ 3. Morgan (Logging)                                      │   │
│  │ 4. Body Parser                                           │   │
│  │ 5. Rate Limiter (100 req/15min)                         │   │
│  └──────────────────────────────────────────────────────────┘   │
│                             │                                     │
│                             ▼                                     │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                      Routes Layer                         │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ GET  /health                                             │   │
│  │ GET  /api/movies/search?title=...                       │   │
│  │ GET  /api/movies/:id                                     │   │
│  │ GET  /api/movies/title/:title                           │   │
│  └──────────────┬───────────────────────────────────────────┘   │
│                 │                                                 │
│                 ▼                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                   Controllers Layer                       │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ - Validate Input                                         │   │
│  │ - Check Cache                                            │   │
│  │ - Call Services                                          │   │
│  │ - Format Response                                        │   │
│  │ - Handle Errors                                          │   │
│  └──────────────┬───────────────────────────────────────────┘   │
│                 │                                                 │
│                 ▼                                                 │
│  ┌────────────────────────┐    ┌─────────────────────────┐      │
│  │   Cache Service        │◄───┤   OMDB Service          │      │
│  ├────────────────────────┤    ├─────────────────────────┤      │
│  │ - In-Memory (LRU)      │    │ - API Integration       │      │
│  │ - Redis (Optional)     │    │ - Request Handler       │      │
│  │ - TTL: 3600s           │    │ - Response Formatter    │      │
│  │ - Max Size: 1000       │    │ - Error Handler         │      │
│  │                        │    │ - Timeout: 10s          │      │
│  │ Cache Keys:            │    └──────────┬──────────────┘      │
│  │ - search:{params}      │               │                      │
│  │ - movie:{id}           │               │                      │
│  │ - title:{title}        │               │                      │
│  └────────────────────────┘               │                      │
│                                            │                      │
└────────────────────────────────────────────┼──────────────────────┘
                                             │
                                             │ HTTPS Requests
                                             ▼
                              ┌──────────────────────────┐
                              │      OMDB API            │
                              │ https://www.omdbapi.com/ │
                              ├──────────────────────────┤
                              │ - Movie Database         │
                              │ - Search Endpoint        │
                              │ - Details Endpoint       │
                              │ - API Key Required       │
                              └──────────────────────────┘
```

## Data Flow Diagrams

### 1. Search Movies Flow

```
User Input (Search)
      │
      ▼
┌─────────────┐
│ SearchBar   │ Enter title, filters
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Home Page   │ handleSearch()
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ API Client  │ searchMovies(title, page, filters)
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Backend API │ GET /api/movies/search?title=...
└──────┬──────┘
       │
       ├─► Check Cache ───────┐
       │                      │
       │                  ┌───▼────┐
       │                  │ Found? │
       │                  └───┬────┘
       │                      │
       │              ┌───────┴────────┐
       │              │                │
       │            YES               NO
       │              │                │
       │              │                ▼
       │              │         ┌──────────────┐
       │              │         │ OMDB Service │
       │              │         │ Fetch Data   │
       │              │         └──────┬───────┘
       │              │                │
       │              │                ▼
       │              │         ┌──────────────┐
       │              │         │ Cache Result │
       │              │         └──────┬───────┘
       │              │                │
       │              └────────────────┘
       │                      │
       ▼                      ▼
┌──────────────────────────────┐
│    Format & Return JSON      │
└──────────────┬───────────────┘
               │
               ▼
┌──────────────────────────────┐
│     Frontend Receives        │
│     - movies[]               │
│     - totalResults           │
│     - page, totalPages       │
└──────────────┬───────────────┘
               │
               ▼
┌──────────────────────────────┐
│   Update State & Render      │
│   - MovieGrid                │
│   - Pagination               │
└──────────────────────────────┘
```

### 2. Movie Detail Flow

```
User Clicks Movie Card
      │
      ▼
┌─────────────────┐
│ Navigate to     │ /movie/:id
│ Detail Page     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ MovieDetail     │ useEffect: Fetch data
│ Component       │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ API Client      │ getMovieById(id)
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Backend API     │ GET /api/movies/:id
└────────┬────────┘
         │
         ├─► Check Cache
         │         │
         │    ┌────▼────┐
         │    │ Cached? │
         │    └────┬────┘
         │         │
         │   ┌─────┴─────┐
         │  YES          NO
         │   │            │
         │   │            ▼
         │   │    ┌──────────────┐
         │   │    │ Fetch from   │
         │   │    │ OMDB API     │
         │   │    └──────┬───────┘
         │   │           │
         │   │           ▼
         │   │    ┌──────────────┐
         │   │    │ Cache Data   │
         │   │    └──────┬───────┘
         │   │           │
         │   └───────────┘
         │         │
         ▼         ▼
┌─────────────────────────────┐
│   Return Full Movie Data    │
│   - Title, Plot, Cast       │
│   - Ratings, Awards, etc.   │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│   Render Detail View        │
│   - Poster                  │
│   - Info Cards              │
│   - Ratings                 │
│   - Favorite Button         │
└─────────────────────────────┘
```

### 3. Favorites Flow

```
User Clicks Heart Icon
      │
      ▼
┌──────────────────┐
│ toggleFavorite() │ From useFavorites hook
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Check if exists  │ isFavorite(imdbID)
└────────┬─────────┘
         │
    ┌────┴────┐
  YES        NO
    │         │
    ▼         ▼
┌────────┐ ┌────────┐
│ Remove │ │  Add   │
└───┬────┘ └───┬────┘
    │          │
    └────┬─────┘
         │
         ▼
┌──────────────────┐
│ Update State     │ setFavorites(...)
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Save to          │ localStorage.setItem()
│ localStorage     │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Update UI        │ Re-render components
└──────────────────┘
```

## Component Hierarchy

```
App
│
├─── Header
│    ├── Logo
│    └── Navigation
│         ├── Home Link
│         └── Favorites Link (with badge)
│
├─── Routes
│    │
│    ├─── Home Page
│    │    ├── Hero Section
│    │    ├── SearchBar
│    │    │   ├── Input
│    │    │   ├── Type Filter
│    │    │   ├── Year Filter
│    │    │   └── Search Button
│    │    ├── MovieGrid
│    │    │   └── MovieCard (×N)
│    │    │       ├── Poster
│    │    │       ├── Title
│    │    │       ├── Year & Type
│    │    │       └── Favorite Button
│    │    └── Pagination
│    │
│    ├─── Movie Detail Page
│    │    ├── Back Button
│    │    ├── Poster
│    │    ├── Movie Info
│    │    │   ├── Title & Favorite
│    │    │   ├── Meta Info
│    │    │   ├── Rating
│    │    │   ├── Genres
│    │    │   ├── Plot
│    │    │   ├── Details Grid
│    │    │   └── Ratings List
│    │
│    └─── Favorites Page
│         ├── Page Title
│         └── Favorites Grid
│             └── Favorite Card (×N)
│                 ├── Poster
│                 ├── Title
│                 ├── Year & Type
│                 └── Remove Button
```

## State Management

```
Global State (Context)
├── FavoritesContext
│   ├── favorites: Movie[]
│   ├── addFavorite(movie)
│   ├── removeFavorite(id)
│   ├── isFavorite(id)
│   └── toggleFavorite(movie)

Local State (useState)
├── Home Page
│   ├── movies: Movie[]
│   ├── loading: boolean
│   ├── error: string | null
│   ├── searchQuery: string
│   ├── currentPage: number
│   ├── totalPages: number
│   └── currentFilters: {}
│
├── Movie Detail Page
│   ├── movie: Movie | null
│   ├── loading: boolean
│   └── error: string | null
│
└── SearchBar
    ├── query: string
    └── filters: { year, type }
```

## Technology Stack

```
Frontend
├── React 18
├── React Router v6
├── Vite (Build Tool)
├── Axios (HTTP Client)
├── Framer Motion (Animations)
└── React Icons

Backend
├── Node.js v16+
├── Express.js
├── Axios (OMDB API)
├── Redis (Optional Cache)
├── Helmet (Security)
├── CORS
├── Express Rate Limit
└── Morgan (Logging)

Development
├── npm/yarn
├── ESLint (Ready)
├── Git
└── VS Code
```

## API Cache Strategy

```
Cache Key Format
├── search:{title}:{page}:{year}:{type}
├── movie:{imdbID}:{plot}
└── title:{title}:{year}:{plot}

Cache Types
├── In-Memory (Default)
│   ├── LRU Eviction
│   ├── Max Size: 1000
│   └── TTL: 3600s
└── Redis (Optional)
    ├── Distributed
    ├── Persistent
    └── Scalable

Cache Flow
1. Request Arrives
2. Generate Cache Key
3. Check Cache
   ├── Hit  → Return Cached Data
   └── Miss → Fetch from API
4. Store in Cache
5. Return Response
```

## Security Layers

```
Security Stack
├── Environment Variables
│   └── API_KEY (Hidden)
├── Rate Limiting
│   └── 100 req/15min per IP
├── Helmet
│   ├── XSS Protection
│   ├── CSRF Protection
│   └── Security Headers
├── CORS
│   └── Allowed Origins
├── Input Validation
│   └── Sanitization
└── Error Handling
    └── No Sensitive Data
```

## Deployment Architecture

```
Production Setup

Frontend (Static Files)
├── Vercel / Netlify
├── CDN Distribution
└── HTTPS

Backend (Node.js)
├── Heroku / Railway
├── Redis Instance
├── SSL Certificate
├── Load Balancer (optional)
└── Monitoring

Database (Future)
├── MongoDB Atlas
└── PostgreSQL
```

---

**This architecture ensures:**
- ✅ Scalability
- ✅ Performance
- ✅ Security
- ✅ Maintainability
- ✅ User Experience
